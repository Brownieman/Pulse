import 'package:flutter/material.dart';

class AppTheme {
  static const Color primaryColor = Color(0xFF2B4AF9);
  static const Color successColor = Color(0xFF22C55E);
  static const Color errorColor = Color(0xFFE53935);

  // Text colors (kept to match existing usage names)
  static const Color textPrimaryColor = Color(0xFF111111);
  static const Color textSecoundaryColor = Color(0xFF6B7280);

  // Surface colors
  static const Color borderColor = Color(0xFFE5E7EB);
  static const Color cardColor = Colors.white;
}
